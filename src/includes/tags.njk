<div class="grid grid-flow-row grid-cols-3 gap-4 mb-2">
    {% set tags = [
        "homelab"
    ] %}
    {% for tag in tags %}
        <a class="tag {{ tag }} mx-auto" href="{{ site.baseUrl }}tag/{{ tag }}">{{ tag }}</a>
    {% endfor %}
</div>
